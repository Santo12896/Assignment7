const Bookmark = ({bookmark}) => {
    const {course_name,price,credit} = bookmark;

    return (
        <div>
            <h1>{course_name}</h1>
            
        </div>
    );
};



export default Bookmark;

